     <div class="card">
        <div class="card-header">
          Edit Post
        </div>
        <div class="card-body">
          
      <div class="container container-fluid mt-5">
        <div class="row justify-content-center">

  
            <label for="newTitle">Title:</label>
            <textarea class="form-control" id="editTitle" data-id="{{id}}"></textarea>
  
  
            <label for="newContent">Content:</label>
            <textarea class="form-control" id="editContent" data-id="{{id}}"></textarea>
  
            <button class="btn btn-primary" id="editBtn" data-id="{{id}}">Update Post</button><br />
            <button class="btn btn-primary" id="deletebtn" data-id="{{id}}">Delete </button>
  
        </div>
      </div>
        </div>
      </div>
      
  <script src="/js/editBlog.js"></script>